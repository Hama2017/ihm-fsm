pragma solidity ^0.8.0;contract Automata0 {enum State {INITIAL,ETAT1,ETAT2,COMPLETED}State currentState = State.INITIAL;function etat2_to_completed() public {require(currentState == State.ETAT2);bool transitionExecuted = false;if (currentState == State.ETAT2) {currentState = State.COMPLETED;transitionExecuted = true;}require(transitionExecuted, "No valid transition conditions met");}function etat2_to_etat1() public {require(currentState == State.ETAT2);bool transitionExecuted = false;if (currentState == State.ETAT2) {currentState = State.ETAT1;transitionExecuted = true;}require(transitionExecuted, "No valid transition conditions met");}function initial_to_etat1() public {require(currentState == State.INITIAL);bool transitionExecuted = false;if (currentState == State.INITIAL) {currentState = State.ETAT1;transitionExecuted = true;}require(transitionExecuted, "No valid transition conditions met");}function etat1_to_etat2() public {require(currentState == State.ETAT1);bool transitionExecuted = false;if (currentState == State.ETAT1) {currentState = State.ETAT2;transitionExecuted = true;}require(transitionExecuted, "No valid transition conditions met");}function get_current_state() public view returns (uint) { return uint(currentState); }function is_completed() public view returns (bool) { return currentState == State.COMPLETED; }function to_completed() public {currentState = State.COMPLETED;}}