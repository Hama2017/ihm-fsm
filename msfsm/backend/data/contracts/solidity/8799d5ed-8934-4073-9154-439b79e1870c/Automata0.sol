pragma solidity ^0.8.0;contract Automata0 {enum State {ETAT_INITIAL,NOUVEL,NOUVEL_ETAT,COMPLETED}State currentState = State.ETAT_INITIAL;function valider() public {require(currentState == State.ETAT_INITIAL);bool transitionExecuted = false;if (currentState == State.ETAT_INITIAL) {currentState = State.NOUVEL_ETAT;transitionExecuted = true;}require(transitionExecuted, "No valid transition conditions met");}function reporter() public {require(currentState == State.NOUVEL_ETAT);bool transitionExecuted = false;if (currentState == State.NOUVEL_ETAT) {currentState = State.NOUVEL;transitionExecuted = true;}require(transitionExecuted, "No valid transition conditions met");}function get_current_state() public view returns (uint) { return uint(currentState); }function is_completed() public view returns (bool) { return currentState == State.COMPLETED; }function to_completed() public { currentState = State.COMPLETED; }}